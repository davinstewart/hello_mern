#!/bin/bash

# Install babel to use as the transpiler.
npm install --save-dev babel-core babel-loader babel-preset-react

# Create .babelrc in my root folder with the following contents
printf "{presets: ['react'] }" | tee ./.babelrc
